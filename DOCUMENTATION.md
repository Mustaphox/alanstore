# ALAN Store - Documentation 📚

## نظرة عامة على الهيكلية البرمجية

يعتمد المتجر على بنية كلاسيكية (Vanilla PHP) بدون استخدام أطر عمل خارجية (Frameworks) لضمان أقصى سرعة ممكنة وأقل استهلاك للموارد، مما يجعله مثالياً للاستضافات المشتركة.

### المجلدات الأساسية:
- `api/`: يحتوي على واجهات برمجة التطبيقات لمعالجة السلة (Cart) والطلبات (Orders) والشحن (Shipping).
- `assets/` & `css/` & `js/`: ملفات التصميم والجافاسكريبت.
- `includes/`: يحتوي على الأجزاء المشتركة مثل الهيدر، الفوتر، دوال المساعدة `functions.php`، وملف الاتصال بالقاعدة `database.php`.
- `admin/`: لوحة تحكم المتجر لإدارة المخزون والطلبيات.

## إعدادات هامة (Configuration)

يتم التحكم في الإعدادات الأساسية عبر ملف `config.php`:
- `DB_HOST`, `DB_NAME`, `DB_USER`, `DB_PASS`: بيانات اتصال قاعدة البيانات.
- `APP_URL`: الرابط الأساسي للموقع (يُترك فارغاً للتعرف التلقائي في أغلب الأحيان).

## التوجيهات (Routing & .htaccess)
يعتمد الموقع على ملف `.htaccess` لإخفاء امتداد `.php` من الروابط، مما يجعل الروابط أكثر احترافية:
- `example.com/shop` بدلاً من `example.com/shop.php`
- هذا التوجيه يتم تلقائياً عبر دالة `base()` الموجودة في `functions.php`.

## نظام التوصيل
يتم جلب بيانات الشحن (أسعار التوصيل للمنزل والمكتب) بناءً على الولاية المختارة، وذلك من خلال طلب `AJAX` إلى الملف `api/shipping.php`.
يتم تحديث السعر الإجمالي مباشرة في واجهة المستخدم في صفحة `checkout`.

## حماية المتجر
- يتم التحقق من بيانات العميل (CSRF Token) في جميع طلبات الـ POST.
- يتم فلترة جميع المدخلات لمنع هجمات XSS.
- يتم إخفاء الملفات الحساسة (`.sql`, `config.php`) عبر قواعد `.htaccess`.
